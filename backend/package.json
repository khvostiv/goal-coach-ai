{
  "name": "backend",
  "version": "1.0.0",
  "description": "CDK backend for autonomous Bedrock Agent task tracker",
  "private": true,
  "scripts": {
    "package:lambdas": "npm install --prefix lambda/proxy --omit=dev && npm install --prefix lambda/action-group --omit=dev",
    "synth": "npx cdk synth",
    "deploy": "npm run package:lambdas && npx cdk deploy",
    "bootstrap": "npx cdk bootstrap"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "aws-cdk": "^2.1129.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
